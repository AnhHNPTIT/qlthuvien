@extends('layouts.app') @section('title',$category->exists?'Sửa thể loại':'Thêm thể loại')
@section('content')<div class="card p-4"><h1 class="h3">{{ $category->exists?'Sửa thể loại':'Thêm thể loại' }}</h1><form method="POST" action="{{ $category->exists?route('categories.update',$category):route('categories.store') }}">@csrf @if($category->exists)@method('PUT')@endif<div class="mb-3"><label class="form-label">Tên thể loại</label><input class="form-control" name="name" value="{{ old('name',$category->name) }}" required></div><div class="mb-3"><label class="form-label">Mô tả</label><textarea class="form-control" name="description" rows="5">{{ old('description',$category->description) }}</textarea></div><button class="btn btn-primary">Lưu</button> <a class="btn btn-outline-secondary" href="{{ route('categories.index') }}">Hủy</a></form></div>@endsection
