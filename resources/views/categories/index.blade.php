@extends('layouts.app') @section('title','Thể loại')
@section('content')<div class="d-flex justify-content-between mb-3"><h1 class="h3">Quản lý thể loại</h1><a class="btn btn-primary" href="{{ route('categories.create') }}">+ Thêm thể loại</a></div><div class="card"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Tên</th><th>Mô tả</th><th>Số sách</th><th></th></tr></thead><tbody>@forelse($categories as $category)<tr><td>{{ $category->name }}</td><td>{{ Str::limit($category->description,60) }}</td><td>{{ $category->books_count }}</td><td class="text-end"><a class="btn btn-sm btn-outline-secondary" href="{{ route('categories.edit',$category) }}">Sửa</a><form class="d-inline" method="POST" action="{{ route('categories.destroy',$category) }}" onsubmit="return confirm('Xóa thể loại này?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Xóa</button></form></td></tr>@empty<tr><td colspan="4" class="text-center py-4">Chưa có thể loại.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $categories->links() }}</div>@endsection
