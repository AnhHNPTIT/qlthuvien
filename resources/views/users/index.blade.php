@extends('layouts.app') @section('title','Người dùng')
@section('content')<h1 class="h3 mb-3">Quản lý người dùng</h1><form class="card p-3 mb-3" method="GET"><div class="row g-2"><div class="col-md-7"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Mã, tên hoặc email"></div><div class="col-md-3"><select class="form-select" name="status"><option value="">Tất cả trạng thái</option>@foreach(['active'=>'Hoạt động','locked'=>'Bị khóa','deleted'=>'Đã xóa'] as $value=>$label)<option value="{{ $value }}" @selected(request('status')===$value)>{{ $label }}</option>@endforeach</select></div><div class="col-md-2 d-grid"><button class="btn btn-outline-primary">Tìm kiếm</button></div></div></form><div class="card"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Mã</th><th>Họ tên</th><th>Email</th><th>Vai trò</th><th>Trạng thái</th><th></th></tr></thead><tbody>@forelse($users as $user)<tr><td><strong>{{ $user->user_code }}</strong></td><td>{{ $user->name }}</td><td>{{ $user->email }}</td><td><span class="badge text-bg-info">{{ $user->role==='admin'?'Admin':'Độc giả' }}</span></td><td><span class="badge {{ ['active'=>'text-bg-success','locked'=>'text-bg-warning','deleted'=>'text-bg-secondary'][$user->status] }}">{{ ['active'=>'Hoạt động','locked'=>'Bị khóa','deleted'=>'Đã xóa'][$user->status] }}</span></td><td><a class="btn btn-sm btn-outline-primary" href="{{ route('users.show',$user) }}">Chi tiết</a></td></tr>@empty<tr><td colspan="6" class="text-center py-5">Không có người dùng.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $users->links() }}</div>@endsection
