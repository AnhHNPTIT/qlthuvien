@extends('layouts.app') @section('title','Quản lý mượn trả')
@section('content')<h1 class="h3 mb-3">Quản lý mượn – trả</h1><form class="card p-3 mb-3" method="GET"><div class="row g-2"><div class="col-md-5"><input class="form-control" name="q" value="{{ request('q') }}" placeholder="Mã độc giả hoặc mã sách"></div><div class="col-md-3"><select class="form-select" name="status"><option value="">Tất cả trạng thái</option>@foreach(['borrowing'=>'Đang mượn','overdue'=>'Quá hạn','returned'=>'Đã trả'] as $value=>$label)<option value="{{ $value }}" @selected(request('status')===$value)>{{ $label }}</option>@endforeach</select></div><div class="col-md-2 form-check d-flex align-items-center ps-5"><input class="form-check-input me-2" type="checkbox" name="unpaid" value="1" id="unpaid" @checked(request('unpaid'))><label for="unpaid">Chưa thu phạt</label></div><div class="col-md-2 d-grid"><button class="btn btn-outline-primary">Lọc</button></div></div></form>
<div class="card"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Độc giả</th><th>Sách</th><th>Mượn/Hạn</th><th>Trạng thái</th><th>Tiền phạt</th><th></th></tr></thead><tbody>@forelse($records as $record)<tr><td><strong>{{ $record->user->user_code }}</strong><br><small>{{ $record->user->name }}</small></td><td><strong>{{ $record->book->book_code }}</strong><br><small>{{ $record->book->title }}</small></td><td>{{ $record->borrow_date->format('d/m/Y') }}<br><small class="text-muted">{{ $record->due_date->format('d/m/Y') }}</small></td><td><span class="badge {{ ['borrowing'=>'text-bg-primary','overdue'=>'text-bg-danger','returned'=>'text-bg-success'][$record->status] }}">{{ ['borrowing'=>'Đang mượn','overdue'=>'Quá hạn','returned'=>'Đã trả'][$record->status] }}</span>@if($record->return_date)<br><small>{{ $record->return_date->format('d/m/Y') }}</small>@endif</td><td>{{ number_format($record->fine_amount) }}đ<br>@if($record->fine_amount>0)<span class="badge {{ $record->is_paid?'text-bg-success':'text-bg-warning' }}">{{ $record->is_paid?'Đã thu':'Chưa thu' }}</span>@endif</td><td class="text-nowrap">@if($record->status!=='returned')<form method="POST" action="{{ route('borrow.return',$record) }}" onsubmit="return confirm('Xác nhận đã nhận lại sách?')">@csrf @method('PATCH')<label class="small"><input type="checkbox" name="is_paid" value="1"> Đã thu phạt</label><br><button class="btn btn-sm btn-success mt-1">Xác nhận trả</button></form>@elseif($record->fine_amount>0&&!$record->is_paid)<form method="POST" action="{{ route('borrow.pay',$record) }}">@csrf @method('PATCH')<button class="btn btn-sm btn-warning">Xác nhận thu phạt</button></form>@endif</td></tr>@empty<tr><td colspan="6" class="text-center py-5">Không có phiếu phù hợp.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $records->links() }}</div>@endsection
