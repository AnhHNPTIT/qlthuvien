@extends('layouts.app') @section('title','Lịch sử mượn')
@section('content')<h1 class="h3 mb-3">Lịch sử mượn của tôi</h1><div class="card"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Mã sách</th><th>Tên sách</th><th>Ngày mượn</th><th>Hạn trả</th><th>Ngày trả</th><th>Trạng thái</th><th>Tiền phạt</th></tr></thead><tbody>@forelse($records as $record)<tr><td>{{ $record->book->book_code }}</td><td>{{ $record->book->title }}</td><td>{{ $record->borrow_date->format('d/m/Y') }}</td><td>{{ $record->due_date->format('d/m/Y') }}</td><td>{{ $record->return_date?->format('d/m/Y') ?? '—' }}</td><td><span class="badge {{ ['borrowing'=>'text-bg-primary','overdue'=>'text-bg-danger','returned'=>'text-bg-success'][$record->status] }}">{{ ['borrowing'=>'Đang mượn','overdue'=>'Quá hạn','returned'=>'Đã trả'][$record->status] }}</span></td><td>{{ number_format($record->fine_amount) }}đ @if($record->fine_amount>0)<span class="badge {{ $record->is_paid?'text-bg-success':'text-bg-warning' }}">{{ $record->is_paid?'Đã thu':'Chưa thu' }}</span>@endif</td></tr>@empty<tr><td colspan="7" class="text-center py-5">Bạn chưa có lịch sử mượn sách.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $records->links() }}</div>@endsection
