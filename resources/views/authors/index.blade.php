@extends('layouts.app') @section('title','Tác giả')
@section('content')<div class="d-flex justify-content-between mb-3"><h1 class="h3">Quản lý tác giả</h1><a class="btn btn-primary" href="{{ route('authors.create') }}">+ Thêm tác giả</a></div><div class="card"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Tên</th><th>Số sách</th><th></th></tr></thead><tbody>@forelse($authors as $author)<tr><td>{{ $author->name }}</td><td>{{ $author->books_count }}</td><td class="text-end"><a class="btn btn-sm btn-outline-secondary" href="{{ route('authors.edit',$author) }}">Sửa</a><form class="d-inline" method="POST" action="{{ route('authors.destroy',$author) }}" onsubmit="return confirm('Xóa tác giả này?')">@csrf @method('DELETE')<button class="btn btn-sm btn-outline-danger">Xóa</button></form></td></tr>@empty<tr><td colspan="3" class="text-center py-4">Chưa có tác giả.</td></tr>@endforelse</tbody></table></div></div><div class="mt-3">{{ $authors->links() }}</div>@endsection
